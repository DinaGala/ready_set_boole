#ifndef ADD_HPP
#define ADD_HPP

#include <cstdint>

// Adds two 32-bit natural numbers using only bitwise operators and assignment.
// Returns the sum as uint32_t.
uint32_t adder(uint32_t a, uint32_t b);

#endif
